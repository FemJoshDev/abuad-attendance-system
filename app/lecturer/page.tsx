"use client";

import { useEffect, useState } from "react";
import Link from "next/link";

type Course = { id: string; courseCode: string; courseTitle: string; studentCount: number; sessionCount: number; latestSession: { id: string; date: string; isOpen: boolean } | null };
type Dashboard = { assignedCourses: number; attendanceSessions: number; studentAttendancePercentage: number | null; courses: Course[] };

export default function LecturerDashboard() {
  const [data, setData] = useState<Dashboard | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");
  useEffect(() => { fetch("/api/lecturer/dashboard").then(async (response) => { if (!response.ok) throw new Error(); return response.json(); }).then((payload) => setData(payload.data)).catch(() => setError("Unable to load lecturer dashboard.")).finally(() => setLoading(false)); }, []);
  if (loading) return <main className="min-h-screen bg-[var(--background)] p-6 text-[var(--ink)]"><p className="text-sm text-[var(--muted)]">Loading dashboard...</p></main>;
  if (error) return <main className="min-h-screen bg-[var(--background)] p-6 text-[var(--ink)]"><h1 className="font-[Manrope] text-2xl font-bold text-[var(--primary)]">Lecturer Portal</h1><p className="mt-4 text-sm text-[var(--error)]" role="alert">{error}</p></main>;

  const stats = [
    { label: "Assigned courses", value: data?.assignedCourses ?? 0, detail: "Active teaching assignments" },
    { label: "Attendance sessions", value: data?.attendanceSessions ?? 0, detail: "Sessions across your courses" },
    { label: "Average attendance", value: data?.studentAttendancePercentage === null ? "No data" : `${data?.studentAttendancePercentage ?? 0}%`, detail: "Present or late records" },
  ];

  return <main className="min-h-screen bg-[var(--background)] text-[var(--ink)]"><div className="flex min-h-screen flex-col md:flex-row"><aside className="hidden w-64 shrink-0 bg-[var(--primary)] p-6 text-white md:flex md:flex-col"><div><p className="font-[Manrope] text-xl font-extrabold tracking-wide">ABUAD</p><p className="mt-1 text-xs text-[#f2dfe0]">Lecturer Portal</p></div><nav className="mt-12 grid gap-2" aria-label="Lecturer navigation">{[["Dashboard", "/lecturer/dashboard"], ["My Courses", "/lecturer/courses"], ["Complaints", "/lecturer/complaints"], ["Notifications", "/notifications"], ["Settings", "/settings"]].map(([label, href], index) => <Link className={`rounded-md px-4 py-3 text-sm font-semibold transition-colors ${index === 0 ? "bg-white/15 text-white" : "text-[#f8ebeb] hover:bg-white/10"}`} href={href} key={href}>{label}</Link>)}</nav><p className="mt-auto border-t border-white/20 pt-5 text-xs text-[#e0c8c9]">Attendance sessions and assigned complaints</p></aside><section className="flex-1"><header className="border-b border-[var(--outline-light)] px-6 py-6 md:px-10"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--outline)]">Lecturer Portal</p><div className="mt-2 flex flex-wrap items-end justify-between gap-4"><div><h1 className="font-[Manrope] text-3xl font-extrabold text-[var(--primary)]">Dashboard</h1><p className="mt-2 text-sm text-[var(--muted)]">Manage your assigned courses and attendance sessions.</p></div><nav className="flex flex-wrap gap-2 text-sm md:hidden" aria-label="Lecturer navigation">{[["Dashboard", "/lecturer/dashboard"], ["My Courses", "/lecturer/courses"], ["Complaints", "/lecturer/complaints"], ["Notifications", "/notifications"], ["Settings", "/settings"]].map(([label, href]) => <Link className="rounded-md border border-[var(--outline-light)] bg-[var(--surface)] px-3 py-2 font-semibold text-[var(--primary)]" href={href} key={href}>{label}</Link>)}</nav></div></header><div className="mx-auto max-w-7xl px-6 py-8 md:px-10"><div className="grid gap-4 md:grid-cols-3">{stats.map((stat) => <article className="rounded-lg border border-[var(--outline-light)] bg-[var(--surface)] p-5 shadow-sm" key={stat.label}><p className="text-xs font-bold uppercase tracking-[0.12em] text-[var(--outline)]">{stat.label}</p><p className="mt-4 font-[Manrope] text-3xl font-extrabold text-[var(--primary)]">{stat.value}</p><p className="mt-2 text-sm text-[var(--muted)]">{stat.detail}</p></article>)}</div><div className="mt-10 flex items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[var(--outline)]">Teaching load</p><h2 className="mt-2 font-[Manrope] text-2xl font-extrabold text-[var(--ink)]">My Courses</h2></div><Link className="rounded-md bg-[var(--primary)] px-4 py-2 text-sm font-bold text-white transition-colors hover:bg-[var(--primary-container)]" href="/lecturer/courses">View all courses</Link></div><div className="mt-5 grid gap-4 lg:grid-cols-2">{data?.courses.map((course) => <article className="rounded-lg border border-[var(--outline-light)] bg-[var(--surface)] p-5 shadow-sm transition hover:-translate-y-0.5 hover:border-[var(--primary)]" key={course.id}><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-bold text-[var(--primary)]">{course.courseCode}</p><h3 className="mt-1 font-[Manrope] text-lg font-bold">{course.courseTitle}</h3></div><span className={`rounded-full px-3 py-1 text-xs font-bold ${course.latestSession?.isOpen ? "bg-[#d9f3e5] text-[#176b3a]" : "bg-[var(--surface-low)] text-[var(--muted)]"}`}>{course.latestSession ? course.latestSession.isOpen ? "Open" : "Closed" : "No sessions"}</span></div><div className="mt-6 grid grid-cols-2 gap-4 border-t border-[var(--surface-high)] pt-4 text-sm"><p><span className="block text-xs uppercase tracking-wide text-[var(--outline)]">Students</span><strong className="mt-1 block">{course.studentCount}</strong></p><p><span className="block text-xs uppercase tracking-wide text-[var(--outline)]">Sessions</span><strong className="mt-1 block">{course.sessionCount}</strong></p></div><Link className="mt-5 inline-flex rounded-md border border-[var(--primary)] px-3 py-2 text-sm font-bold text-[var(--primary)] hover:bg-[var(--primary)] hover:text-white" href={`/lecturer/courses/${course.id}`}>Open attendance controls</Link></article>)}</div></div></section></div></main>;
}
